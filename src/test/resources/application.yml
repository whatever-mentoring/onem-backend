spring:
  datasource:
    url: jdbc:h2:mem:~/myApp
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: embedded
      schema-locations: classpath:schema.sql
  profiles:
    active: test


logging:
  level:
    org:
      springframework:
        jdbc: debug